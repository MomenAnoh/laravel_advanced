services:
    laravel-app:
        container_name: laravel-app
        build: .
        ports:
            - "8000:8000"
        depends_on:
            - database
        environment:
          - APP_ENV=production
          - APP_DEBUG=false



